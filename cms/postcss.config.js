// Standalone PostCSS config for the CMS app.
// No plugins — Payload manages its own CSS pipeline.
// Adding plugins like autoprefixer can break Payload's @layer CSS.
module.exports = {
  plugins: {},
}
